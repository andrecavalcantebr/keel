Notas do caso.

Comentários dos esperados
=========================

Prosa que abria os arquivos esperados, movida para cá porque o cgen gera
só o cabeçalho de uma linha (desenho do cgen §5.3).

**`app/app_cx.c`** (c11)

gerado de app/cx.k, perfil C11: `constexpr` sai verbatim e os
usos não mudam. Sob C11 o de bloco vira macro de nome gerado, com os usos
reescritos — e `c->N` fica intacto (§9.2).

**`main_app_cx.c`** (c23, c11)

unidade de ponto de entrada, gerada por `--main app.cx`
(backend §5.8).

**`app/app_cx.c`** (c23)

gerado de app/cx.k, perfil C23: `constexpr` sai verbatim e os
usos não mudam. Sob C11 o de bloco vira macro de nome gerado, com os usos
reescritos — e `c->N` fica intacto (§9.2).
