/* keel.proto.h — gerado de keel.k, perfil C23. `module keel;` não declara
   verbo, só tipos — a interface é literalmente incluir o `.type.h`, sem
   protótipo nenhum (backend §4.3.2). Normativo: keel-c-backend.md §4.2. */
#ifndef KEEL_PROTO_H
#define KEEL_PROTO_H
#include "keel.type.h"

#endif /* KEEL_PROTO_H */
