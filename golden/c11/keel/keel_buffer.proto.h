/* keel/keel_buffer.proto.h — o que o módulo `keel.buffer` declara uma vez, e não por
   instância: o cursor guarda uma posição e não menciona `T` (backend §5.11). */
#ifndef KEEL_KEEL_BUFFER_PROTO_H
#define KEEL_KEEL_BUFFER_PROTO_H
#include "keel/keel_buffer.type.h"

#endif /* KEEL_KEEL_BUFFER_PROTO_H */
