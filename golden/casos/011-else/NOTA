O que este caso prova
=====================

As duas formas da cláusula `else` (§5.12), executando:
  * SAÍDA — o statement entra no `if`, verbatim; a variante com bloco também;
  * DEFAULT — a expressão entra no `win` da instância e é reatribuída ao
    próprio símbolo, sem temporário;
  * o teste é sempre `M_failed(x)`, nunca `if (!x)`;
  * NONE também é falha: `dobro(0)` sai pela cláusula, como `dobro(-1)`.

E a instância recursiva `outcome slice i32`, que exercita o mangling do §2.2.

Prova também `slice.length(outcome.value(s))` — verbo aplicado a verbo que
devolve valor. Ela funciona porque `slice` não é `byref` e o acessor recebe
cópia (§4.11). Enquanto os acessores de `slice` recebiam ponteiro, esta forma
dava `incompatible type for argument 1`, e não havia lowering especificado.

Comentários dos esperados
=========================

Prosa que abria os arquivos esperados, movida para cá porque o cgen gera
só o cabeçalho de uma linha (desenho do cgen §5.3).

**`app/app_el.c`** (c23, c11)

§5.12: a declaração sai como estava, seguida de um `if` cujo teste é o
`failed` da instância — nunca `if (!x)`. Tudo numa linha só (regra 2 do §6).
