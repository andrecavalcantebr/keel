/* gen/main_app_cx.c — unidade de ponto de entrada, gerada por `--main app.cx`
   (backend §5.8). */

#include "keel/keel.type.h"
#include "app/app_cx.impl.h"
int main(int argc, char **argv) { return app_cx_main(argc, argv); }
