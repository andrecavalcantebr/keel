/* gen/grid.h — o par do módulo genérico, praticamente vazio: tudo que ele
   declara menciona um parâmetro, então pertence à instância (backend §4.3). */
#ifndef GRID_H
#define GRID_H
#include "grid.type.h"

#endif /* GRID_H */
