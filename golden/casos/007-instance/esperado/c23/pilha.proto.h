/* pilha.proto.h — o `.h` do módulo genérico, uma vez. A constante não menciona
   parâmetro nem modificador, então não pertence a instância nenhuma e o nome
   não leva o argumento (backend §4.4.1). */
#ifndef PILHA_PROTO_H
#define PILHA_PROTO_H
#include "pilha.type.h"

constexpr i32 pilha_VAZIA = -1;
#endif /* PILHA_PROTO_H */
