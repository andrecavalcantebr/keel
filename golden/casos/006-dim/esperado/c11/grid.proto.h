/* grid.proto.h — o par do módulo genérico, praticamente vazio: tudo que ele
   declara menciona um parâmetro, então pertence à instância (backend §4.3). */
#ifndef GRID_PROTO_H
#define GRID_PROTO_H
#include "grid.type.h"

#endif /* GRID_PROTO_H */
