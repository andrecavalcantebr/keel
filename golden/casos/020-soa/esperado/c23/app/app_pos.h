/* gen/app/pos.h — gerado de app/pos.k, perfil C23.
   Só o que é `pub`: o protótipo de `somar_x` (backend §4.1). */
#ifndef APP_APP_POS_H
#define APP_APP_POS_H
#include "app/app_pos.type.h"

typedef struct keel_arena keel_arena;
keel_outcome_i32 app_pos_somar_x(keel_arena *a, size_t n);
#endif /* APP_APP_POS_H */
