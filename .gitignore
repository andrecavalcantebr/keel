# gerado por ./gerar-ccjson.sh — tem caminhos absolutos, não versionar
compile_commands.json

# a árvore da base, gerada dos .k de tools/codegen/base por tools/codegen
/gen/
/tools/codegen/gen/

# build
obj/
*.out

# o executável do projeto. ANCORADO na raiz de propósito: sem a barra, `keel`
# casaria todo diretório com esse nome — src/base/keel/, golden/c23/keel/ — e
# engoliria a base inteira sem avisar.
/keel
