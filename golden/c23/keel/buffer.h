/* keel/buffer.h — o que o módulo `keel.buffer` declara uma vez, e não por
   instância: o cursor guarda uma posição e não menciona `T` (backend §5.11). */
#ifndef KEEL_BUFFER_H
#define KEEL_BUFFER_H
#include "keel/buffer.type.h"

#endif /* KEEL_BUFFER_H */
