/* gen/app/pool.h — gerado de app/pool.k, perfil C11. */
#ifndef APP_POOL_H
#define APP_POOL_H
#include "app/pool.type.h"

typedef struct arena arena;
bool app_pool_inicia(arena *a);
#endif /* APP_POOL_H */
