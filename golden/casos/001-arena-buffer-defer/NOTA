Notas do caso.

Comentários dos esperados
=========================

Prosa que abria os arquivos esperados, movida para cá porque o cgen gera
só o cabeçalho de uma linha (desenho do cgen §5.3).

**`app/app_cfg.h`** (c23, c11)

Só o que é `pub`: os protótipos, sobre o `.type.h` (backend §4.1).

**`app/app_cfg.type.h`** (c23, c11)

Só o que é `pub`: o tipo e a constante — `constexpr` de módulo é L1
(backend §4.1, §4.3.1).
