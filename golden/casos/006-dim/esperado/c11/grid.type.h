/* gen/grid.type.h — o par do módulo genérico, praticamente vazio: tudo que ele
   declara menciona um parâmetro, então pertence à instância (backend §4.3). */
#ifndef GRID_TYPE_H
#define GRID_TYPE_H
#include "keel/prelude.h"

#endif /* GRID_TYPE_H */
